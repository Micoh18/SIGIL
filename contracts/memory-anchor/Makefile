build-contract:
	@echo "memory-anchor is currently a source/spec stub. Install Rust + cargo-casper, then replace this stub with a buildable Casper contract."
	@exit 1

test:
	@echo "memory-anchor tests are blocked until Rust + cargo-casper are available."
	@exit 1
