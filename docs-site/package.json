{
  "name": "@sigil/docs-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:llms": "node scripts/generate-llms.mjs",
    "dev": "npm run docs:llms && vitepress dev docs --host 127.0.0.1",
    "build": "npm run docs:llms && vitepress build docs",
    "preview": "vitepress preview docs --host 127.0.0.1"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
